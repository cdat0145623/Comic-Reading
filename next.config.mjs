/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "static.cdnno.com",
            "lh3.googleusercontent.com",
            "res.cloudinary.com",
            "authjs.dev",
        ],
    },
};

export default nextConfig;
