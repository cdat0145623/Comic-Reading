import Spinner from "./Spinner";

function RouteLoadingSpinner() {
    return (
        <div className="mt-6 min-h-64 flex items-center justify-center">
            <Spinner />
        </div>
    );
}

export default RouteLoadingSpinner;
